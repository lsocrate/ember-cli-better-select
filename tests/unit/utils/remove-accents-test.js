import removeAccents from '../../../utils/remove-accents';
import { module, test } from 'qunit';

module('Unit | Utility | remove accents');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = removeAccents();
  assert.ok(result);
});
